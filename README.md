This is how you can recreate the environment for the project:
(needs to be confirmed)

```
virtualenv --no-site-packages --distribute .env && source .env/bin/activate && pip install -r requirements.txt
```